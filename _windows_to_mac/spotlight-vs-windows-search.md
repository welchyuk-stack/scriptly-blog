---
layout: post
title: "Spotlight vs Windows Search"
excerpt: "Same idea — press a key, start typing, find what you need. Spotlight goes further than most people realize."
author: Luke Welch
section: windows_to_mac
---

# Spotlight vs Windows Search

Windows Search (`Win` key, then just start typing) and Spotlight (`⌘+Space`) solve the same basic problem: find something fast without navigating menus. The shortcut to open it and the intent behind it feel similar. What Spotlight actually does once it's open goes further than most switchers expect.

## The basics, same as Windows Search

`⌘+Space` opens Spotlight anywhere, anytime. Start typing an app name, hit Enter on the first result, and it launches — functionally identical to typing an app name into the Windows Start menu search.

Same for files: type a filename or partial name, and matching files across your whole Mac show up, sorted by relevance and recency.

## Where it goes past "just search"

**It's a calculator.** Type `458 * 12` directly into Spotlight and the answer appears inline, no Enter needed, no separate app opened. Same for basic unit conversions — type `10 km to miles` and it resolves it directly.

**It defines words.** Type a word and Spotlight often shows a dictionary definition preview without leaving the search box.

**It searches file contents, not just names.** Because Spotlight is backed by a system-wide content index (the same one Finder's search uses), searching for a phrase can surface a PDF or document that *contains* that phrase, even if the filename has nothing to do with it. Windows Search can do this too in more recent versions, but it's less consistently reliable across file types than Spotlight tends to be.

**It converts currency**, using rates that update automatically as long as you're online — type `50 usd to gbp`.

**It's a quick launcher for System Settings pages.** Type "Displays" or "Bluetooth" and Spotlight jumps straight to that specific settings pane, skipping the navigation entirely.

## What it doesn't do as well

Spotlight's web search integration is more limited than Windows Search's — it'll offer a "Search the Web" option that opens your default browser, but it doesn't show rich web results inline the way Windows 11's search sometimes does. If you want that, that's what a browser's own address bar is for on Mac — most browsers treat the address bar as a combined search/URL bar already.

## A genuinely underused feature: Spotlight in Finder windows

Separate from the system-wide `⌘+Space` Spotlight, `⌘+F` inside a Finder window runs a similar search scoped to whatever folder you're currently browsing (or your whole Mac, toggleable). It supports filters too — click the `+` button in a Finder search to filter by file kind, date modified, size, and more, without typing search syntax.

## The habit worth building

Most experienced Mac users barely touch the Dock or Applications folder to launch apps — `⌘+Space`, type the first few letters, Enter, is faster for almost everything once it's automatic. If you're still clicking through folders to find apps a few weeks in, that's the single highest-value habit to switch over.

For the exact shortcut alongside every other Windows-to-Mac keyboard mapping, see the [full shortcuts reference](/windows-to-mac/keyboard-shortcuts/).
